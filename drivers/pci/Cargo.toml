[package]
name = "pci"
version.workspace = true
edition.workspace = true

[lib]
path = "src/pci.rs"

[dependencies]
bit-utils = { path = "../../crates/bit-utils" }
x86-port = { path = "../x86-port" }
