[workspace]
resolver = "3"
members = ["abi", "compiler", "init", "kernel", "shell", "testing"]

[workspace.package]
version = "0.1.0"
edition = "2024"

[workspace.dependencies]
abi = { path = "abi" }
kernel = { path = "kernel" }
